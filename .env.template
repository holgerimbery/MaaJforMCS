# Copy this file to .env and fill in your values.
# NEVER commit .env to git — it is already in .gitignore.

# Azure AI Foundry / Azure OpenAI — required
JUDGE_ENDPOINT=https://your-resource.openai.azure.com/
JUDGE_API_KEY=your-api-key-here
JUDGE_MODEL=gpt-4o

# Authentication — set to true to enable Entra ID login
AUTHENTICATION_ENABLED=false

# Entra ID — only required when AUTHENTICATION_ENABLED=true
# AZUREAD_INSTANCE defaults to https://login.microsoftonline.com/ — only override for sovereign clouds
# AZUREAD_INSTANCE=https://login.microsoftonline.com/
AZURE_TENANT_ID=your-tenant-id
AZURE_CLIENT_ID=your-client-id
AZURE_CLIENT_SECRET=your-client-secret

# NOTE: register these redirect URIs in your App Registration:
#   https://<your-host>/signin-oidc
#   https://<your-host>/signout-callback-oidc
