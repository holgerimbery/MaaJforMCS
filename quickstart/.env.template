# ── AI Judge (required) ──────────────────────────────────────────────────────
JUDGE_ENDPOINT=https://your-resource.openai.azure.com/
JUDGE_API_KEY=your-api-key-here
JUDGE_MODEL=gpt-4o

# ── Authentication ────────────────────────────────────────────────────────────
# AUTHENTICATION_ENABLED=false  →  no login required, all users get Admin access.
#   Use this only on a trusted internal network or behind a VPN.
#   NEVER set to false if the app is reachable from the internet.
#
# AUTHENTICATION_ENABLED=true   →  Entra ID SSO with RBAC (Admin/Tester/Viewer).
#   Requires the three AZURE_* values below.
#   See: https://github.com/holgerimbery/MaaJforMCS/wiki/Entra-ID-Setup
AUTHENTICATION_ENABLED=false
AZURE_TENANT_ID=
AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=
